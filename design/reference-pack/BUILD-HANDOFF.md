# Authorized UI vertical slice
User now explicitly asks to divide agentic coding work and get started on next UI step. Earlier UI-session doc says no implementation: that is superseded for this bounded LOCAL slice. Do not modify research or numerical code. No commits, push, deploy, account changes, or spending. Other session owns simulation/. Root is not a git repository. You are the sole application writer, only under /Users/vaibhav/hackcmu/ui/ (create it). Reports in that directory. Do not edit other app directories.

Build a WORKING local Next.js + TypeScript UI, real orbital 3D (Three.js or react-three-fiber), linked SVG schematic, selection tree, exact/sliding parameter editing and real FastAPI results. Use port 3100 (3000 is occupied by an unrelated service) and same-origin Next API proxy to existing http://127.0.0.1:8000. Backend simulation/api.py and engine.py are read-only. Hermes will run backend. No fake solver or fallback numerical data.
Read design/reference-pack/CLAUDE-CHATGPT-PROMPT.md and REFERENCES.md, inspect reference-board.jpg using your image/file tool, and engine.py/api.py. Current user design takes precedence over stale graphite/fixed-schematic proposals in docs/ui-ux-session-handoff.md. User wants SolidWorks-like approachable CAD, full orbit AND flat schematic, novice-accessible, real-time edit feedback. Neutral gray canvas, named tree, central object, right inspector, bottom results. No landing page. Polished but tool-like. Have 3D/Schematic/Split; genuine orbit/pan/zoom and reset. Selected junction/capacitor cross-highlights. Substrate illustrative only. Technical details progressively disclosed. Energy levels and plots from returned arrays only. Live updates debounce and guard against stale responses. Honest updating/error/empty states. Correct null dispersion < reporting floor handling. Independent EJ/EC in free explore, no pretend frequency lock. Backend defaults EJ15 EC0.3 ng0. Interpret names plainly with exact scientific meaning available.

Minimum complete slice: example automatically evaluated, component selection, editing EJ/EC/ng, actual updated metrics and levels, reset, pin baseline and comparison. Fit narrow widths without crashing. Optional if core stable: modest exploded assembly slider (view only, separated named meshes), /search constraints and preview/apply. Do not sacrifice working core for these. Material picker/geometry physics deferred; visible materials are illustrative, do not invent physical material performance. Disclose simplified model quietly.

Install local dependencies in ui only. Run build/typecheck and focused tests for result formatting/selection/stale handling where practical. Add package scripts. Do not leave dead buttons. No unsupported auto quantum particle animation.
Write ui/IMPLEMENTATION-REPORT.md with exact implemented interactions, commands/tests, known gaps, how to start, and next decisions. Finish only when build passes or explicit actionable blocker. End final output QUBIT_UI_SLICE_DONE. Do not start long-lived servers yourself; Hermes will run and inspect the UI after your build. Work efficiently, aim for first functional slice rather than a giant architecture.
