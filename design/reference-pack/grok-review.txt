I'll read the four handoff files first, then review the authorized slice as a read-only UX/model-contract check.The handoff is clear; next I’ll pull the API contract and any existing UI/session notes so the checklist matches the authorized slice, not extra science.I’ll pull the v1 model contract and engine tests next so pass conditions match actual `/evaluate` behavior, not the older graphite-schematic docs.I have the model contract; consulting Hermes on the authorized slice so the checklist stays scoped to free-explore evaluate, not the locked-search demo.