# Copy to ui/.env.local. Never commit real keys.

# Gemini — server-side only. Ask AI turns on when this is set.
GEMINI_API_KEY=

# Optional. Default is gemini-2.5-flash.
# GEMINI_MODEL=gemini-2.5-flash

# Solver location if it is not on 127.0.0.1:8000.
# QUBIT_API_URL=http://127.0.0.1:8000
